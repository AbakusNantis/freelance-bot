from utils.DefaultTable import DefaultTable

class ListManager:
    """
    Contains methods for
    - saving new items to the list with date
    - flagging items
    - deleting items
    """
    
    def __init__(self, table = DefaultTable()):
        return
    
    def save_items_to_list(self, new_items: list[str]):
        return
    
    def flag_item(url: str, column_name: str, flag):
        return
    
